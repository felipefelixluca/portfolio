# build output
dist/
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.local
.env.*.local

# macOS / Windows
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# editor swap / backup files
*.swp
*.swo
*~
*.bak
*.orig

# TypeScript incremental cache
*.tsbuildinfo

# Test / coverage output (in case tests are added later)
coverage/
.nyc_output/

# Claude Code local (user-specific) settings — never commit
.claude/settings.local.json

# StatiCrypt private key files, if ever used (secrets — never commit)
.staticrypt-keys/

# StatiCrypt backups
*.staticrypt-bak.html
